# Generated by mirage configure -t unix (Mon, 1 May 2017 08:42:36 GMT).

-include Makefile.user

OPAM = opam
DEPEXT ?= opam depext --yes mirage-unikernel-hello_world-unix

.PHONY: all depend depends clean build
all:: build

depend depends::
	$(OPAM) pin add --no-action --yes mirage-unikernel-hello_world-unix .
	$(DEPEXT)
	$(OPAM) install --yes --deps-only mirage-unikernel-hello_world-unix
	$(OPAM) pin remove --no-action mirage-unikernel-hello_world-unix

build::
	mirage build

clean::
	mirage clean

